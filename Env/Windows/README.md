Unfortunately, the docker container on Windows platform does not support CUDA.

You can install all the dependencies manually using the requirements.txt file provided in this folder
in your favorable virtual environment (venv / conda).
