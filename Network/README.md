This directory is for third-party network we accelerated on.

It contains the minimal changes to the network itself, the actual acceleration
is performed by run-time dynamic injection (a.k.a. monkey patching) by the methods
in /Accelerate directory.
